#include <stdio.h>
#include <stdlib.h>
int main() {
    int *p, *q, i, n;
    printf("Enter size: ");
    scanf("%d", &n);
    p = (int*)malloc(n * sizeof(int));
    q = (int*)calloc(n, sizeof(int));
    printf("Memory allocated successfully");
    free(p);
    free(q);
    return 0;
}


