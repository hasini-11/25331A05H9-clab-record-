#include <stdio.h>
#include <stdlib.h>
int main() {
    int *p, n;
    p = (int*)malloc(3 * sizeof(int));
    p = (int*)realloc(p, 5 * sizeof(int));
    printf("Memory resized successfully");
    free(p);
    return 0;
}

